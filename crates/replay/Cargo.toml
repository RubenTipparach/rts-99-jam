[package]
name = "replay"
description = "Record and replay the command stream. With a deterministic sim, seed + commands fully reconstruct a match."
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
protocol = { path = "../protocol" }

[lints.rust]
unsafe_code = "forbid"
