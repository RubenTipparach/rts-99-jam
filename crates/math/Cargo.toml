[package]
name = "math"
description = "Deterministic fixed-point scalar and vector math for the simulation core."
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true

[lints.rust]
unsafe_code = "forbid"
