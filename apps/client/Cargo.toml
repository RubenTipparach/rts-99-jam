[package]
name = "client"
description = "Game client: a wgpu + winit renderer driving the deterministic sim. Builds native and for the web (WASM)."
edition.workspace = true
version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# Simulation side of the wall (deterministic, fixed-point).
math = { path = "../../crates/math" }
protocol = { path = "../../crates/protocol" }
sim = { path = "../../crates/sim" }
# Presentation side: floats/GPU allowed here.
wgpu = { version = "29", features = ["webgl"] }
winit = "0.30"
glam = "0.33"
bytemuck = { version = "1", features = ["derive"] }
image = { version = "0.25", default-features = false, features = ["png"] }
log = "0.4"
web-time = "1"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
pollster = "0.4"
env_logger = "0.11"

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "0.2"
wasm-bindgen-futures = "0.4"
console_error_panic_hook = "0.1"
console_log = "1"
web-sys = { version = "0.3", features = [
    "Document",
    "Window",
    "Element",
    "HtmlCanvasElement",
    "CanvasRenderingContext2d",
    "EventTarget",
    "Event",
    "DomTokenList",
] }
